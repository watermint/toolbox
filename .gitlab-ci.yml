
build:
  image: docker:stable
  services:
  - docker:dind
  stage: build
  script:
  - mkdir dist
  - export
  - docker build -t toolbox .
  - docker run -e TOOLBOX_BUILD_ID=$CI_PIPELINE_IID -e TOOLBOX_APP_CREDENTIALS='$TOOLBOX_APP_CREDENTIALS' -v $PWD/dist:/dist --rm toolbox
  artifacts:
    paths:
    - $PWD/dist
